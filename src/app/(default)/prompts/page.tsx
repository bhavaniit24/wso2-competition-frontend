import { PromptContent } from '@/common';

const Prompts = () => {
    return (
        <div className="flex flex-wrap h-full px-4 sm:px-6">
            <PromptContent />
        </div>
    );
};

export default Prompts;
