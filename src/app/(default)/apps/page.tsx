import { AppsContent } from '@/common';

const Apps = () => {
    return (
        <div className="flex flex-wrap h-full px-4 sm:px-6">
            <AppsContent />
        </div>
    );
};

export default Apps;
