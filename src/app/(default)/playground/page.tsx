import { ChatContent } from '@/common';

const Playground = () => {
    return (
        <div className="flex flex-wrap h-full px-4 sm:px-6">
            <ChatContent />
        </div>
    );
};

export default Playground;
